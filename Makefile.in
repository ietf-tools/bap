#
# $Id:$
#

OBJS=	parser.o scanner.o main.o @LIBOBJS@

CFLAGS=	@CFLAGS@ @DEFS@ -DYYDEBUG
YACC=	@YACC@ -d -v
CC=	@CC@

bap:	${OBJS}
	${CC} -o $@ ${DEBUG} ${OBJS} -ll

scanner.c:	scanner.l
parser.c:	parser.y

clean:
	rm -f bap scanner.c parser.c $(OBJS)
